[Unit]
Description=The Choria Orchestrator Server
After=network.target

[Service]
StandardOutput=syslog
StandardError=syslog
User=root
Group=root
ExecStart={{bindir}}/{{pkgname}} server --config={{etcdir}}/server.cfg

[Install]
WantedBy=multi-user.target
